// Update when releasing a new extension version
export const VERSION = '0.8.0';
export const PKG_NAME = 'locatorforge';
export const CHROME_ZIP_PATH = `/downloads/${PKG_NAME}-chrome-v${VERSION}.zip`;
export const EDGE_ZIP_PATH = `/downloads/${PKG_NAME}-edge-v${VERSION}.zip`;
